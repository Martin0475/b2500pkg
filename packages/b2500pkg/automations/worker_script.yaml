input_number:
  b2500_01_status:
    name: B2500_01 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_02_status:
    name: B2500_02 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_03_status:
    name: B2500_03 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_04_status:
    name: B2500_04 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_05_status:
    name: B2500_05 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_06_status:
    name: B2500_06 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_07_status:
    name: B2500_07 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_08_status:
    name: B2500_08 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

  b2500_09_status:
    name: B2500_09 Status
    min: -1
    max: 4
    step: 1
    mode: box
    initial: 0

script:
  b2500_01_worker:
    alias: B2500PKG_B01_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b01_topic') }}/{{ states('input_text.b01_typ') }}/App/{{ states('input_text.b01_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b01_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_01/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_01/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_01/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_01/CD16/last_updated
            {% else %}
              B2500PKG/B2500_01/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_01_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_01_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_01_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_01_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_01_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_01_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_01_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_01_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_01_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_01_status
                data:
                  value: -1

  b2500_02_worker:
    alias: B2500PKG_B02_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b02_topic') }}/{{ states('input_text.b02_typ') }}/App/{{ states('input_text.b02_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b02_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_02/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_02/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_02/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_02/CD16/last_updated
            {% else %}
              B2500PKG/B2500_02/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_02_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_02_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_02_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_02_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_02_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_02_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_02_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_02_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_02_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_02_status
                data:
                  value: -1

  b2500_03_worker:
    alias: B2500PKG_B03_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b03_topic') }}/{{ states('input_text.b03_typ') }}/App/{{ states('input_text.b03_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b03_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_03/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_03/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_03/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_03/CD16/last_updated
            {% else %}
              B2500PKG/B2500_03/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_03_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_03_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_03_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_03_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_03_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_03_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_03_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_03_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_03_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_03_status
                data:
                  value: -1

  b2500_04_worker:
    alias: B2500PKG_B04_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b04_topic') }}/{{ states('input_text.b04_typ') }}/App/{{ states('input_text.b04_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b04_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_04/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_04/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_04/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_04/CD16/last_updated
            {% else %}
              B2500PKG/B2500_04/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_04_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_04_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_04_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_04_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_04_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_04_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_04_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_04_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_04_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_04_status
                data:
                  value: -1

  b2500_05_worker:
    alias: B2500PKG_B05_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b05_topic') }}/{{ states('input_text.b05_typ') }}/App/{{ states('input_text.b05_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b05_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_05/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_05/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_05/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_05/CD16/last_updated
            {% else %}
              B2500PKG/B2500_05/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_05_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_05_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_05_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_05_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_05_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_05_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_05_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_05_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_05_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_05_status
                data:
                  value: -1

  b2500_06_worker:
    alias: B2500PKG_B06_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b06_topic') }}/{{ states('input_text.b06_typ') }}/App/{{ states('input_text.b06_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b06_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_06/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_06/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_06/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_06/CD16/last_updated
            {% else %}
              B2500PKG/B2500_06/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_06_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_06_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_06_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_06_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_06_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_06_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_06_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_06_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_06_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_06_status
                data:
                  value: -1

  b2500_07_worker:
    alias: B2500PKG_B07_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b07_topic') }}/{{ states('input_text.b07_typ') }}/App/{{ states('input_text.b07_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b07_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_07/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_07/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_07/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_07/CD16/last_updated
            {% else %}
              B2500PKG/B2500_07/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_07_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_07_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_07_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_07_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_07_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_07_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_07_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_07_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_07_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_07_status
                data:
                  value: -1

  b2500_08_worker:
    alias: B2500PKG_B08_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b08_topic') }}/{{ states('input_text.b08_typ') }}/App/{{ states('input_text.b08_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b08_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_08/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_08/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_08/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_08/CD16/last_updated
            {% else %}
              B2500PKG/B2500_08/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_08_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_08_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_08_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_08_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_08_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_08_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_08_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_08_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_08_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_08_status
                data:
                  value: -1

  b2500_09_worker:
    alias: B2500PKG_B09_Worker
    mode: queued
    max: 50

    fields:
      payload:
        required: true
        selector:
          text:
      followup_payload:
        required: false
        default: ""
        selector:
          text:
      no_check:
        required: false
        default: false
        selector:
          boolean:
      expected_answer:
        required: false
        default: {}
        selector:
          object:

    sequence:
      - variables:
          mqtt_topic: "{{ states('input_text.b09_topic') }}/{{ states('input_text.b09_typ') }}/App/{{ states('input_text.b09_mac') }}/ctrl"
          mqtt_mac: "{{ states('input_text.b09_mac') }}"
          payload_clean: "{{ payload | default('', true) | string | trim }}"
          followup_payload_clean: "{{ followup_payload | default('', true) | string | trim }}"
          no_check_clean: "{{ no_check | default(false) }}"
          expected_answer_clean: >-
            {% if expected_answer is defined and expected_answer is mapping %}
              {{ expected_answer }}
            {% else %}
              {{ {} }}
            {% endif %}
          wait_command: "{{ followup_payload_clean if followup_payload_clean != '' else payload_clean }}"
          wait_topic: >-
            {% if wait_command == 'cd=01' %}
              B2500PKG/B2500_09/CD01/last_updated
            {% elif wait_command == 'cd=13' %}
              B2500PKG/B2500_09/CD13/last_updated
            {% elif wait_command == 'cd=15' %}
              B2500PKG/B2500_09/CD15/last_updated
            {% elif wait_command == 'cd=16' %}
              B2500PKG/B2500_09/CD16/last_updated
            {% else %}
              B2500PKG/B2500_09/CD01/last_updated
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ payload_clean == "" }}'
        then:
          - stop: "Kein payload → Abbruch"

      - if:
          - condition: template
            value_template: '{{ mqtt_mac in ["", "unknown", "unavailable", "none", None] }}'
        then:
          - stop: "MAC ungültig → Abbruch"

      - if:
          - condition: template
            value_template: >-
              {{ expected_answer is defined
                 and expected_answer is not mapping }}
        then:
          - stop: "expected_answer ist kein Dictionary → Abbruch"

      - service: input_number.set_value
        target:
          entity_id: input_number.b2500_09_status
        data:
          value: 1

      - service: mqtt.publish
        data:
          topic: "{{ mqtt_topic }}"
          payload: "{{ payload_clean }}"
          qos: 1

      - if:
          - condition: template
            value_template: '{{ followup_payload_clean != "" }}'
        then:
          - delay:
              seconds: 2
          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ followup_payload_clean }}"
              qos: 1

      - if:
          - condition: template
            value_template: '{{ no_check_clean == true }}'
        then:
          - delay:
              milliseconds: 500
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_09_status
            data:
              value: 4
          - stop: "no_check aktiv → gesendet, ohne Prüfung"

      - wait_for_trigger:
          - trigger: mqtt
            topic: "{{ wait_topic }}"
        timeout: "00:00:05"
        continue_on_timeout: true

      - variables:
          first_ok: >-
            {% if wait.trigger is none %}
              false
            {% else %}
              {% set ns = namespace(ok=true) %}
              {% for field, expected in expected_answer_clean.items() %}
                {% set field_name = field | string | lower %}
                {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                  {% set entity = 'binary_sensor.b2500_09_' ~ field_name %}
                  {% set actual = 1 if is_state(entity, 'on') else 0 %}
                {% else %}
                  {% set entity = 'sensor.b2500_09_' ~ field_name %}
                  {% set actual = states(entity) %}
                {% endif %}
                {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                  {% set ns.ok = false %}
                {% elif expected is number %}
                  {% if actual | float(999999999) != expected | float(999999998) %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% else %}
                  {% if actual | string != expected | string %}
                    {% set ns.ok = false %}
                  {% endif %}
                {% endif %}
              {% endfor %}
              {{ ns.ok }}
            {% endif %}

      - if:
          - condition: template
            value_template: '{{ first_ok }}'
        then:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_09_status
            data:
              value: 3
        else:
          - service: input_number.set_value
            target:
              entity_id: input_number.b2500_09_status
            data:
              value: 2

          - service: mqtt.publish
            data:
              topic: "{{ mqtt_topic }}"
              payload: "{{ payload_clean }}"
              qos: 1

          - if:
              - condition: template
                value_template: '{{ followup_payload_clean != "" }}'
            then:
              - delay:
                  seconds: 2
              - service: mqtt.publish
                data:
                  topic: "{{ mqtt_topic }}"
                  payload: "{{ followup_payload_clean }}"
                  qos: 1

          - wait_for_trigger:
              - trigger: mqtt
                topic: "{{ wait_topic }}"
            timeout: "00:00:05"
            continue_on_timeout: true

          - variables:
              second_ok: >-
                {% if wait.trigger is none %}
                  false
                {% else %}
                  {% set ns = namespace(ok=true) %}
                  {% for field, expected in expected_answer_clean.items() %}
                    {% set field_name = field | string | lower %}
                    {% if field_name in ['d1', 'd2', 'd3', 'd4', 'd5'] %}
                      {% set entity = 'binary_sensor.b2500_09_' ~ field_name %}
                      {% set actual = 1 if is_state(entity, 'on') else 0 %}
                    {% else %}
                      {% set entity = 'sensor.b2500_09_' ~ field_name %}
                      {% set actual = states(entity) %}
                    {% endif %}
                    {% if actual in ['unknown', 'unavailable', 'none', ''] %}
                      {% set ns.ok = false %}
                    {% elif expected is number %}
                      {% if actual | float(999999999) != expected | float(999999998) %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% else %}
                      {% if actual | string != expected | string %}
                        {% set ns.ok = false %}
                      {% endif %}
                    {% endif %}
                  {% endfor %}
                  {{ ns.ok }}
                {% endif %}

          - if:
              - condition: template
                value_template: '{{ second_ok }}'
            then:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_09_status
                data:
                  value: 3
            else:
              - service: input_number.set_value
                target:
                  entity_id: input_number.b2500_09_status
                data:
                  value: -1

  b2500pkg_worker_sequence:
    alias: B2500PKG Worker Sequenz
    description: >
      Ruft einen ausgewählten B2500-Worker mit mehreren Payloads
      nacheinander auf.
    mode: parallel
    max: 9

    fields:
      worker:
        name: Worker
        description: B2500-Worker, der verwendet werden soll
        required: true
        selector:
          select:
            options:
              - label: B01
                value: script.b2500_01_worker
              - label: B02
                value: script.b2500_02_worker
              - label: B03
                value: script.b2500_03_worker
              - label: B04
                value: script.b2500_04_worker
              - label: B05
                value: script.b2500_05_worker
              - label: B06
                value: script.b2500_06_worker
              - label: B07
                value: script.b2500_07_worker
              - label: B08
                value: script.b2500_08_worker
              - label: B09
                value: script.b2500_09_worker

      payloads:
        name: Payloads
        description: >
          Payloads, die nacheinander über den Worker gesendet werden.
          Beispiel: ["cd=16", "cd=13"]
        required: true
        selector:
          object:

    sequence:
      # --------------------------------------------------
      # Eingaben aufbereiten
      # --------------------------------------------------
      - variables:
          worker_clean: "{{ worker | string | trim }}"

          allowed_workers:
            - script.b2500_01_worker
            - script.b2500_02_worker
            - script.b2500_03_worker
            - script.b2500_04_worker
            - script.b2500_05_worker
            - script.b2500_06_worker
            - script.b2500_07_worker
            - script.b2500_08_worker
            - script.b2500_09_worker

          payload_list: >-
            {% if payloads is string %}
              {{
                payloads.splitlines()
                | map('trim')
                | reject('eq', '')
                | list
              }}
            {% else %}
              {{ payloads | list }}
            {% endif %}

      # --------------------------------------------------
      # Prüfung
      # --------------------------------------------------
      - if:
          - condition: template
            value_template: >-
              {{
                worker_clean not in allowed_workers
                or payload_list | count == 0
              }}
        then:
          - stop: "Ungültiger Worker oder keine Payloads angegeben"

      # --------------------------------------------------
      # Payloads nacheinander an den Worker geben
      # --------------------------------------------------
      - repeat:
          for_each: "{{ payload_list }}"
          sequence:

            - action: "{{ worker_clean }}"
              data:
                payload: "{{ repeat.item | string | trim }}"
                followup_payload: ""
                no_check: true
                expected_answer: {}

            # Gleicher Abstand wie bisher zwischen
            # Payload und Followup im Worker
            - if:
                - condition: template
                  value_template: "{{ not repeat.last }}"
              then:
                - delay:
                    seconds: 2